# ClawdPet 素材替换与重新分发指南

> 更新：2026-09-13。本文说明当前 GIF 素材系统的规格契约、替换一套素材的正确流程，以及重新构建分发的完整步骤。

## 1. 素材系统现状

- 格式：**透明 GIF（动图）**，像素风格，image-rendering: pixelated
- 数量：**13 张**，单皮肤（outfit 只有 `a`）
- 画布：**302×207**，全部统一高度（2026-09-13 起裁掉顶部 93px 空白；原因是点击热区=图片矩形，顶部空白会让螃蟹上方一大块区域挡点击）
- 底边对齐：各动作的"脚底"必须贴画布底部区域一致，渲染层用 `bottom:10px` 锚定，高度不齐会导致动作切换时跳动

### 姿态与文件名契约（{pose}-a.gif）

| 文件 | 动作 | | 文件 | 动作 |
|------|------|-|------|------|
| idle-a | 待机 | | sit-a | 看书 |
| idle2-a | 待机2 | | sleep-a | 睡觉 |
| wave-a | 挥挥钳 | | shy-a | 蹦跶 |
| cheer-a | 开心跳跃 | | pout-a | 闹脾气 |
| clap-a | 抛接杂耍 | | error-a | 报错惊呆 |
| typing-a | 打字 | | sweep-a | 扫地 |
| groove-a | 戴耳机摇摆 | | | |

- `land`（落地）复用 idle-a，无独立文件
- `walk`（mini-crabwalk）已整体移除，**不要再加回**：该素材满画幅无法统一裁剪，且走路功能已删
- 姿态清单在三处必须一致：`assets/pet/` 目录、`assets/manifest.json`、`tests/assets.test.js` 的 `required` 数组；改姿态须三处同步，renderer/pet.js 的 `IMAGES` 映射按状态取名

## 2. 替换素材的硬性要求

1. 透明背景，内容紧凑：**每个姿态的内容顶部边界距画布顶 ≥3px 即可，严禁大片顶部空白**（会重新放大点击热区）
2. 所有姿态**同一画布高度**（当前 207；若整套素材天然高度不同，统一裁到内容最高姿态 + 10px 余量的公共线）
3. 内容底边对齐（各姿态脚底在同一水平线附近），保持落地震荡动画不穿帮
4. 帧时长随意，循环标记 loop=0（无限循环）

### 检测与裁剪脚本（PIL）

```python
# 测量每张 GIF 各帧非透明内容的上下边界
from PIL import Image
im = Image.open("xxx-a.gif")
top, bottom = im.height, 0
for i in range(im.n_frames):
    im.seek(i)
    b = im.convert("RGBA").getbbox()
    if b: top, bottom = min(top, b[1]), max(bottom, b[3])
print(top, bottom)
```

批量统一裁顶部（示例 CUT=93）：

```python
frames, durs = [], []
for i in range(im.n_frames):
    im.seek(i); durs.append(im.info.get("duration", 80))
    frames.append(im.convert("RGBA").crop((0, CUT, im.width, im.height)))
frames[0].save("out.gif", save_all=True, append_images=frames[1:],
               duration=durs, loop=0, disposal=2, optimize=True)
```

注：PIL 的 optimize 会合并完全相同的相邻帧（帧数下降但视觉与总时长不变），属正常。

## 3. 替换后的静态检查（必做）

1. **manifest.json**：`{w, h, rawW, rawH}` 必须与实际 GIF 尺寸一致（w/h 读 GIF 头即校验，`tests/assets.test.js` 会卡）
2. 姿态清单三处一致（目录/manifest/测试）
3. 逐张验证：帧可解码、尺寸统一、内容 min_top ≥ 3px、loop 无限
4. `npm test` 全过（assets.test 会自动卡掉清单不一致）

## 4. 图标同步（换形象才需要）

四端图标 icon.ico / icon.png / tray.png / clawd-avatar.png 必须同源。当前取 groove 动画第 16 帧（戴耳机摇摆），源图在桌面 `Clawd图标候选/06-groove-a-f16.png`。换形象时从新素材取代表性帧，同时更新这四个文件。

## 5. 重新构建与分发

```bash
cd src
npm test
for f in main.js preload.js lib/*.js renderer/*.js; do node --check "$f"; done
npm run test:electron
rm -rf dist
CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist
node tests/packaged-smoke.js
# 交付：只覆盖 exe，保留用户三件套中的 models.json / AGENTS.md
taskkill //IM ClawdPet.exe //F 2>/dev/null; sleep 1
cp -f dist/ClawdPet-2.0.0.exe "C:/ClawdPet/ClawdPet.exe"
```

分发边界：

- 最终用户产物 = 单文件 `ClawdPet.exe`，无安装器、无密钥
- `C:\ClawdPet\` 里的 `models.json` 与 `AGENTS.md` 是**用户自己的文件**，任何交付动作都不得触碰
- 桌面分发副本由用户自行压缩，助手不产 zip
- 打包坑与禁签名原因见《工程结构与开发指南.md》第 5 节
