# ClawdPet 2.0.0 工程结构与开发指南

> 更新：2026-09-13。本文面向后续维护者与自动化智能体，描述当前工程的最终构成、运行架构与开发/测试/发布全流程。路径均相对本文所在目录。

## 1. 目录结构

```text
./
├─ 工程结构与开发指南.md              # 本文
├─ 素材替换与重新分发指南.md          # 素材规格、替换与重分发操作手册
└─ src/                               # 唯一可编辑的完整源码
   ├─ main.js                         # 主进程：窗口、坐标、物理调度、托盘、右键菜单、聊天窗、微型 Agent 工具执行
   ├─ preload.js                      # 安全预加载桥，只暴露白名单 IPC API
   ├─ package.json                    # 版本、脚本、依赖与 electron-builder 配置
   ├─ afterPack.js                    # 打包后用 rcedit 嵌图标与版本信息（NSIS 封壳前）
   ├─ .npmrc                          # npm 与 Electron 国内镜像
   ├─ .gitignore
   ├─ lib/
   │  ├─ motion.js                    # 坐标、拖拽、边界物理模型（walking 底层代码保留但已无入口触发）
   │  ├─ settings.js                  # 设置校验、原子写入、损坏恢复
   │  ├─ ai.js                        # OpenAI 兼容流式对话 completeStream + function calling 解析
   │  └─ models.js                    # models.json 实时读盘、baseUrl 容错、多档案管理
   ├─ renderer/
   │  ├─ index.html / pet.js / style.css / geometry.js   # 宠物渲染页：状态机、交互、命中上报
   │  ├─ chat.html / chat.js / chat.css                 # 聊天窗：流式渲染、工具卡片、设置面板
   │  └─ vendor/                      # 前端第三方库（marked 等）
   ├─ assets/
   │  ├─ pet/                         # 13 张透明 GIF（302×207，统一裁过顶部空白）
   │  ├─ manifest.json                # 各 GIF 尺寸元数据，必须与实际文件一致
   │  ├─ icon.ico / icon.png / tray.png / clawd-avatar.png   # 四端图标（groove 第 16 帧同源）
   └─ tests/
      ├─ assets.test.js               # 素材/manifest/姿态清单一致性（13 姿态）
      ├─ motion.test.js / edges.test.js / settings.test.js / renderer.test.js
      ├─ electron-smoke.js            # 真实 Electron 集成（姿态/缩放/越界采样）
      └─ packaged-smoke.js            # 便携 EXE 产物冒烟
```

历史产物已清理：旧 `DesktopPet-1.1.1.exe` 已删；`assets/raw/` 绿幕原图已不存在；`tools/process_assets.py` 属 1.x PNG 素材管线，现已不用（保留仅作参考，勿再运行）。

## 2. 用户侧运行布局

程序是单文件便携 EXE，放到任意文件夹即可运行。同目录可放两个可选的用户文件：

| 文件 | 作用 |
|------|------|
| `models.json` | AI 模型档案（必配，否则无法聊天）。只认 EXE 同目录（开发模式落 userData），每次使用实时读盘、零缓存 |
| `AGENTS.md` | Clawd 身份与性格设定，每轮请求实时重读（上限 16KB），缺失则跳过 |

程序内零内置模型、零密钥；升级时只覆盖 EXE，用户这两个文件保持不动。

## 3. 核心行为契约（改代码前必读）

- **完全被动**：无文字气泡、不打招呼；单击=随机动作，双击=开聊天，右键=极简菜单（动作/大小/开关/自启/显隐/退出）
- **无走路**：walk/mini-crabwalk 满画幅素材已随走路功能整体移除（菜单项、随机池、IPC/preload 通道、GIF、manifest 全删）。`motion.js` 底层 walking 代码保留但永无入口
- **悬停物理**：拖到哪停哪，无重力抛飞；贴近地面才落地
- **Alt+C** = 聊天窗开关（hide/show，绝不清空对话）；**Ctrl+Alt+C** = 宠物显隐
- 点击热区 = 渲染层上报的 `<img>` 矩形（hit-rect IPC），GIF 画布高度直接决定可点击范围——素材必须保持紧凑画布（见素材指南）
- 聊天即微型 Agent：read_file / write_file / run_command（PowerShell 5.1），全自动、全盘、100 轮上限；工具交互不持久化进对话史
- 流式中断：插话立即 abort 旧流；世代计数 streamEpoch 使旧回调失效
- 上下文预算 400KB，超限折叠最早工具输出（保前缀缓存命中）；maxTokens 12288

## 4. 开发与测试

```bash
cd src
npm test                    # 38 个纯逻辑单测，须全过
for f in main.js preload.js lib/*.js renderer/*.js; do node --check "$f"; done
npm run test:electron       # 10 项真实渲染集成
```

注意：测前必须 `taskkill /IM ClawdPet.exe /F` 清掉残留实例，否则污染结果。

## 5. 构建与交付（每次改完照抄）

```bash
rm -rf dist
CSC_IDENTITY_AUTO_DISCOVERY=false npm run dist   # 禁签名
node tests/packaged-smoke.js
# 交付：结束正在运行的进程后，只覆盖 exe，保留用户目录里的 models.json / AGENTS.md
taskkill //IM ClawdPet.exe //F 2>/dev/null; sleep 1
cp -f dist/ClawdPet-2.0.0.exe "<部署目录>/ClawdPet.exe"
```

### Windows 打包常见坑

1. winCodeSign 软链接无管理员权限必失败，已配 `win.signAndEditExecutable: false` 绕过
2. 图标由 afterPack.js 在 NSIS 封壳**前**用 rcedit 嵌入；严禁对封壳后的 portable EXE 跑 rcedit（CRC 校验必崩）
3. 改 productName/exe 名会连带 afterPack.js 的 `appInfo.productFilename` 和 packaged-smoke.js 的产物路径
